all:
  vars:
    ansible_port: 22
    ansible_user: ubuntu
  children:
    apis_servers:
      hosts:
        prod-server:
    admin_servers:
      hosts:
        prod-server:
    batch_servers:
      hosts:
        prod-server:
