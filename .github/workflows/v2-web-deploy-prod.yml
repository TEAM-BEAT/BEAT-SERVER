name: v2-web-deploy-prod

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g. 2.0.0)'
        required: true

jobs:
  v2-web-deploy-prod:
    environment: prod
    runs-on: ubuntu-22.04
    permissions:
      contents: read

    env:
      MODULE: apis
      PROFILE: prod
      CONTAINER_NAME: beat-v2-web-prod
      REMOTE_APP_DIR: /opt/beat/beat-v2-web-prod

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: "25"
          distribution: "temurin"

      - name: Gradle caching
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-home-cache-cleanup: true

      - name: Verify v2-web baseline
        run: |
          chmod +x gradlew
          GRADLE_USER_HOME=/tmp/gradle-home ./gradlew verifyV2WebBaseline
        shell: bash

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.PROD_DOCKER_LOGIN_USERNAME }}
          password: ${{ secrets.PROD_DOCKER_LOGIN_ACCESSTOKEN }}

      - name: Compute image name
        id: image
        run: |
          jar_path=$(find apis/build/libs -maxdepth 1 -name 'apis-*.jar' ! -name '*-plain.jar' | head -n 1)
          if [ -z "$jar_path" ]; then
            echo "apis jar not found" >&2
            exit 1
          fi
          VERSION="${{ github.event.inputs.version }}"
          echo "jar_path=${jar_path}" >> "$GITHUB_OUTPUT"
          echo "image=${{ secrets.PROD_DOCKER_LOGIN_USERNAME }}/beat-apis:${VERSION}" >> "$GITHUB_OUTPUT"
          echo "latest=${{ secrets.PROD_DOCKER_LOGIN_USERNAME }}/beat-apis:prod-latest" >> "$GITHUB_OUTPUT"
        shell: bash

      - name: Build and push image
        run: |
          docker build -f Dockerfile.module \
            --build-arg MODULE=${MODULE} \
            --build-arg PROFILE=${PROFILE} \
            --build-arg JAR_PATH="${{ steps.image.outputs.jar_path }}" \
            --platform linux/amd64 \
            -t "${{ steps.image.outputs.image }}" \
            -t "${{ steps.image.outputs.latest }}" \
            .
          docker push "${{ steps.image.outputs.image }}"
          docker push "${{ steps.image.outputs.latest }}"
        shell: bash

      - name: Deploy to prod host
        uses: appleboy/ssh-action@v1.2.0
        env:
          PROD_SECRET_PROPERTIES: ${{ secrets.APPLICATION_PROD_SECRET_SPRING }}
          PROD_V2_ACTUATOR_PORT: ${{ secrets.PROD_V2_ACTUATOR_PORT }}
          PROD_ACTUATOR_PATH: ${{ secrets.PROD_ACTUATOR_PATH }}
          PROD_V2_WEB_PUBLIC_PORT: ${{ secrets.PROD_V2_WEB_PUBLIC_PORT }}
        with:
          host: ${{ secrets.PROD_SSH_HOST }}
          username: ${{ secrets.PROD_SSH_USERNAME }}
          key: ${{ secrets.PROD_SSH_PRIVATE_KEY }}
          port: ${{ secrets.PROD_SSH_PORT }}
          envs: CONTAINER_NAME,REMOTE_APP_DIR,PROD_SECRET_PROPERTIES,PROD_V2_ACTUATOR_PORT,PROD_ACTUATOR_PATH,PROD_V2_WEB_PUBLIC_PORT
          script: |
            mkdir -p /opt/beat/secret
            printf "%s" "$PROD_SECRET_PROPERTIES" > /opt/beat/secret/application-prod-secret.properties
            chmod 700 /opt/beat/secret || true
            chmod 600 /opt/beat/secret/application-prod-secret.properties
            docker pull "${{ steps.image.outputs.image }}"
            docker rm -f ${CONTAINER_NAME} || true
            docker run -d \
              --name ${CONTAINER_NAME} \
              --restart unless-stopped \
              --network beat-network \
              -v /opt/beat/secret:/app/secret:ro \
              -p ${PROD_V2_WEB_PUBLIC_PORT}:8080 \
              -p ${PROD_V2_ACTUATOR_PORT}:2222 \
              "${{ steps.image.outputs.image }}"
            for attempt in $(seq 1 30); do
              if curl --fail --silent "http://localhost:${PROD_V2_ACTUATOR_PORT}${PROD_ACTUATOR_PATH}/health" > /dev/null; then
                exit 0
              fi
              sleep 2
            done
            docker logs --tail 100 ${CONTAINER_NAME} || true
            exit 1
